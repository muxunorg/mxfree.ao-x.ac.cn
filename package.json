{
  "devDependencies": {
    "vitepress": "^1.2.3"
  },
  "scripts": {
    "docs:start": "cnpm install && vitepress dev --host 0.0.0.0",
    "docs:dev": "vitepress dev --host 0.0.0.0",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "docs:serve": "vitepress build && vitepress preview"
  }
}
